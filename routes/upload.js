var express = require('express');
var router = express.Router();
const db = require('../database/dbQuery.js');

router.post('/', function(req, res, next) {
    // TODO: Implement DB function for adding recipe to DB
});

module.exports = router;
